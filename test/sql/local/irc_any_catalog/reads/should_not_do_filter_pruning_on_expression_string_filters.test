# name: test/sql/local/irc_any_catalog/reads/should_not_do_filter_pruning_on_expression_string_filters.test
# group: [reads]

require avro

require parquet

require iceberg

require-env DUCKDB_ICEBERG_HAVE_GENERATED_DATA

statement ok
attach ':memory:' as my_datalake;

statement ok
create schema my_datalake.default;

statement ok
SET unsafe_enable_version_guessing = true;

statement ok
SET enable_logging=true;

statement ok
SET logging_level='trace';

query I
SELECT item_id FROM ICEBERG_SCAN('__WORKING_DIRECTORY__/data/generated/iceberg/spark-local/default/issue_791') WHERE length(item_id) = 36;
----
10000000-0000-0000-0000-000000000001
20000000-0000-0000-0000-000000000002

# Should not have skipped any files based on bound checks, as it's unreliable with expression filters (e.g. len())
query I
select count(*) FROM duckdb_logs() where type = 'Iceberg' and message.contains('Iceberg Filter Pushdown, skipped ''data_file'':');
----
0

query I
SELECT item_id FROM ICEBERG_SCAN('__WORKING_DIRECTORY__/data/generated/iceberg/spark-local/default/issue_791') WHERE length(item_id) > 35 AND length(item_id) < 37;
----
10000000-0000-0000-0000-000000000001
20000000-0000-0000-0000-000000000002

query I
SELECT item_id FROM ICEBERG_SCAN('__WORKING_DIRECTORY__/data/generated/iceberg/spark-local/default/issue_791') WHERE length(item_id) IN (36);
----
10000000-0000-0000-0000-000000000001
20000000-0000-0000-0000-000000000002