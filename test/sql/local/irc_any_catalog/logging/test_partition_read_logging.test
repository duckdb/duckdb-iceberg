# name: test/sql/local/irc_any_catalog/logging/test_partition_read_logging.test
# group: [logging]

require-env ICEBERG_SERVER_AVAILABLE

require-env SECRETS_CREATED_AND_CATALOG_ATTACHED

require avro

require parquet

require iceberg

require httpfs

require icu

require ducklake

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set enable_logging=true

statement ok
SET TimeZone = 'UTC';

statement ok
call enable_logging('Iceberg');

query I
select dt from my_datalake.default.month_partitioned_date where dt = DATE '2025-06-01';
----
2025-06-01

query I
select count(*) from duckdb_logs() where type = 'Iceberg' and regexp_matches(message, '.*skipped.*data_file.*partition column.*with transform.*did not match filter.*dt=.2025-06-01.*');
----
2

