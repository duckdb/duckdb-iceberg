# name: test/sql/local/irc/alter/test_alter_table_simple.test
# group: [alter]

require-env ICEBERG_SERVER_AVAILABLE

require avro

require parquet

require iceberg

require httpfs

statement ok
CREATE SECRET (
    TYPE S3,
    KEY_ID 'admin',
    SECRET 'password',
    ENDPOINT '127.0.0.1:9000',
    URL_STYLE 'path',
    USE_SSL 0
);

statement ok
ATTACH '' AS my_datalake (
    TYPE ICEBERG,
    CLIENT_ID 'admin',
    CLIENT_SECRET 'password',
    ENDPOINT 'http://127.0.0.1:8181'
);

statement ok
drop table if exists my_datalake.default.alter_table_test;

statement ok
CREATE TABLE my_datalake.default.alter_table_test AS (select range a FROM range(10));

statement ok
Alter table my_datalake.default.alter_table_test ADD COLUMN b INT;

statement ok
insert into my_datalake.default.alter_table_test values (11, 20);

query II
select * from my_datalake.default.alter_table_test;
----
0	NULL
1	NULL
2	NULL
3	NULL
4	NULL
5	NULL
6	NULL
7	NULL
8	NULL
9	NULL
11	20


# what about default value support?
# What about nested column types? Adding/removing struct column information.